[package]
name = "pamplemouche-compat"
version = "0.1.0"
edition = "2021"

[lib]
name = "pamplemouche_compat"
path = "src/lib.rs"

[dependencies]
# no_std — all dependencies must also be no_std capable
